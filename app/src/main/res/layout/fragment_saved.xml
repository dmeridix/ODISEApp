<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Título -->
    <TextView
        android:id="@+id/text_saved_places"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Lugares Guardados"
        android:textSize="18sp"
        android:textStyle="bold"
        android:paddingBottom="8dp" />

    <!-- ProgressBar para indicar carga -->
    <ProgressBar
        android:id="@+id/progress_bar_saved"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:visibility="gone" />

    <!-- RecyclerView para los lugares guardados -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_view_saved_places"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scrollbars="vertical" />
</LinearLayout>